item: M-CLI-1
date: 2026-04-12T07:54:40Z
severity: medium
description: Added a Linux-only launch integration regression that assumed `strait session watch --session` would exit within a fixed timeout after `session.stop`, even though the implemented contract was to exit after the targeted session disappears from the registry. The test path was skipped on the local macOS environment because the container gateway binary is not Linux ELF, so CI surfaced the timing mismatch after push.
