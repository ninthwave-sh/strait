item: M-CSM-6
date: 2026-04-16T14:00:00Z
severity: medium
description: Desktop package install and test commands could not use the default node shim because asdf had no active nodejs version in this worktree; every npm command needed an explicit `ASDF_NODEJS_VERSION=22.16.0 asdf exec ...` wrapper.
