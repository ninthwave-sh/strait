item: H-ICDP-3
date: 2026-04-17T13:26:28Z
severity: medium
description: |
  The launch-mode Docker integration test
  `launch_policy_replace_live_updates_running_session` in
  tests/launch_integration.rs is flaky -- it periodically times out waiting
  for PTY JSON output (\"timed out waiting for PTY JSON output\" at
  tests/launch_integration.rs:2677). It failed on my H-ICDP-3 CI run even
  though my changes are entirely in the agent crate and don't touch any
  launch code. It also failed on the previous H-ICDP-1 merge run
  (workflow run 24566471497) but passed on the H-HCP-1 merge, so it
  passes intermittently. Each failure blocks an otherwise-clean PR until
  someone manually re-runs the job. Worth stabilizing as part of M-ICDP-6.
